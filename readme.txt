'dragleave' events aren't firing reliably.  So you'll see some issues with days being the wrong color.

You can start up a webserver to serve static content from the current directory on linux/mac by typing
python -m SimpleHTTPServer 8000
then go to http://localhost:8000

